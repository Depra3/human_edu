EX01 : 하이퍼링크로 폴더간 페이지 이동 - 절대 경로
EX02 : 하이퍼링크로 폴더간 페이지 이동 - 상대 경로
EX03 : 목록(ul(li), ol(li), dl(dt,dd)), 테이블 표(thead,tbody,tr,td) - 병합(colspan-가로 병합(행), rowspan-세로 병합(열))
EX04  - 1 : input(텍스트필드, 패스워드필드, Submit)  textarea, placeholder(텍스트필드에서 예시로 글쓰기)
        2 : Select, ComboBox, Radio Button
        3 : Submit
        4 : hidden
        5 : form
      6-1 : method - get
      6-2 : method - post
        7 : img, video, audio
EX05    1 : 웹문서 페이지 (img, input, div, ul, select, table, dl, header, nav, main, footer)
        2 : 웹문서 페이지 - 영역 구분
EX06 : 블럭 tag, 인라인 tag